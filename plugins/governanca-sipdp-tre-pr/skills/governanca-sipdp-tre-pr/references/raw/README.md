# raw/

Drop source documents here (articles, PDFs, transcripts, notes). This directory is immutable — Claude reads from it but never edits or deletes files here. One file or subfolder per source is fine; name them however makes sense to you, Claude will link to them from `wiki/sources/`.
